const baseUrl = 'https://wiseweblab.com/api/api';

const base = {
    account: baseUrl + '/account',
    article: baseUrl + '/article',
    tag: baseUrl + '/tag',
    face: baseUrl + '/face',
};

export default base;
