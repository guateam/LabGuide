获取权限
用于检测用户是否拥有特定权限
---
tags:
  - right
parameters:
  - in: query
    name: token
    type: string
    description: token
  - in: query
    name: target
    type: string
    description: 生效作用域
  - in: query
    name: right
    type: string
    description: 权限编号
responses:
  200:
    description: 服务器正常启动
    schema:
      id: default
